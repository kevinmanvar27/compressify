# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false

# Use npm registry
registry=https://registry.npmjs.org/

# Disable lockfile check if needed during deployment
# frozen-lockfile=false
