#!/bin/bash
# Pre-install hook - runs before npm install
# Disable corepack to prevent pnpm

export COREPACK_ENABLE_STRICT=0
rm -rf ~/.cache/node/corepack 2>/dev/null || true
echo "Corepack disabled - using npm"
